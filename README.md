# FirstRepo

**steps**
* first download the visual studio 
* next install VS
* in your github create one repo with public
* your repo names should be small case
* open yout gitbash goto the repos folder in your local
* run the clone command
* git clone <repoName> 

Just I renamed dummy file to Todays file